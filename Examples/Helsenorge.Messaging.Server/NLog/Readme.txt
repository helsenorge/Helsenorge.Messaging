These files are copied from https://github.com/NLog/NLog.Extensions.Logging
This Package depends on ASP.NET Core hosting code that doesn't work with .NET 4.6. 

We copy the code so that we can get a working sample of how to integrate NLog with a logging system running on .NET 4.6